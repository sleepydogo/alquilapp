<head>
<%= stylesheet_link_tag 'bootstrap', 'wallet-styles' %>
<link href="https://fonts.googleapis.com/css?family=Nunito:200,200i,300,300i,400,400i,600,600i,700,700i,800,800i,900,900i" rel="stylesheet">

</head>
<p style="color: green"><%= notice %></p>
<p style="color: red"><%= alert %></p>



<%= form_with(model: @rent) do |form| %>
  <% if @rent.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(@rent.errors.count, "error") %> prohibited this rent from being saved:</h2>

      <ul>
        <% @rent.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>






<br>






<div class="container">
                <div class="row m-0">
                    <div class="col-12 px-4">
                        <div class="d-flex align-items-end mt-4 mb-2">

                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <p class="textmuted">Precio por hora de extensión</p>
                            <p class="fs-14 fw-bold"><span class="fas fa-dollar-sign pe-1"></span>2,000</p>		
                        </div>
                        <div class="d-flex justify-content-between mb-3">
                            <p class="textmuted fw-bold">Total</p>
                            <div class="d-flex align-text-top ">
                                <span class="fas fa-dollar-sign mt-1 pe-1 fs-14 "></span><span class="h4">2,000</span>
                            </div>
                        </div>
                    </div>
                    <div class="col-12 px-0">
                        <div class="row bg-light m-0">
                            <div class="col-12 px-4 my-4">
                                <p class="fw-bold">Detalle del alquiler</p>
                            </div>
                            <div class="col-12 px-4">
                                <div class="d-flex  mb-4">
                                    <span class="">
                                        <p class="text-muted">Seleccione la nueva hora de finalizacion del alquiler: </p>
                                        <div class="form-group">
                                        <%= form.time_field :tiempo %>
                                      </div> 
                                    </span>
                                </div>
                                <div class="d-flex mb-5">
                                    <span class="me-5">
                                        <p class="text-muted">Nombre y apellido del inquilino</p>
                                        <%= current_user.name %>
                                    </span>
                                </div>
                            </div>
                        </div>
						<div class="form-check">
  							<input class="form-check-input" type="checkbox" value="" id="flexCheckDefault">
  							<label class="form-check-label" for="flexCheckDefault">
    							Acepto los términos y condiciones de usuario
  							</label>
                        <div class="row m-0">
                            <div class="col-12  mb-4 p-0">
                            <%= form.submit "Extender alquiler", class: 'btn btn-primary' %>
                                </div>
                            </div>
                            <% end %>

                        </div>
						</div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>




